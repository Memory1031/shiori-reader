# PARSE-004：解析收口与设备验收

日期：2026-09-09。自动核查与文档收口已执行，Android 真机主要流程已验收，iOS 用户确认本轮验收收口；PARSE-004 按用户结论完成，不修改版本、不发布。

## 本轮工作

- 将过期的 EPUB 支持矩阵更新为 [1.1.0 当前支持范围](epub-support-profile.md)，核对 GAP-001～012 归属；TXT、重解析和辅助链接均纳入。
- 补强真实临时 SQLite 组合测试：旧链接元数据缺失 → 显式重解析 → 关闭存储和数据库 → 重新打开 → 进度及辅助块恢复，读取辅助内容不修改历史。
- 已有 widget 测试覆盖菜单打开辅助页 → 返回相同 Reader State、临时页不写历史、跳过 linear=no、深度限制。此证据与存储组合测试分开，不称为真机全链路测试。
- 曾生成原创样本用于本轮 Android 验收。用户已有独立验收文件，现已移除专用样本、生成器、三项样本检查及截图；保留通用解析、存储与 Reader 回归。

## 自动验证

- 清理前完整 `flutter test --no-pub --reporter expanded`：559 项通过，包含在线解析的离线 fixture，无真实书源请求。
- `flutter analyze --no-pub`：无问题。
- 清理前格式检查：250 个 Dart 文件，0 改动；`git diff --check HEAD` 通过。
- 自动测试包含临时 SQLite 与 widget；后续 ADB 人机界面验收单列如下。历史 PARSE-005 / 008 数量不与本轮相加。
- 清理前本地化重新生成前后 SHA-256 一致，样本再次生成摘要一致。这些是当次证据，不代表清理后重跑全量测试。
- 本轮未修改 schema；不重复生成 Drift。

## 用户设备验收清单

后续使用用户单独维护的验收文件；表中的具体编号和文字是本轮 Android 历史样本的观察点，不要求用户文件具有相同内容。记录安装版本/提交、系统版本、机型与结果。Android 与 iOS 分开填写，不以另一平台通过代替。可在正常使用设备测试，不需要磁盘写满、清书库或改公司签名。

| 项目 | 操作与预期 | Android | iOS |
| --- | --- | --- | --- |
| 新导入 | 导入三份样本；TXT 各两章，emoji/扩展汉字无乱码，“第十二章的时候”不是目录；EPUB 渐变图可见 | 通过 | 用户整体确认，未逐项反馈 |
| 目录与翻章 | EPUB 目录跳第一章 A、附录、第二章；第一章连续翻页应直接到第二章，跳过附录；观察整页动画与裁剪 | 目录/连续顺序通过；动画帧率未测 | 用户整体确认，未逐项反馈 |
| 脚注返回 | 第一章菜单“本章链接”选“脚注一”，看到解释后“返回原位置”；原页不变；“缺失目标”不可跳转 | 通过 | 用户整体确认，未逐项反馈 |
| 重启续读 | 读到编号 20 附近，正常退出再结束进程并重开；继续阅读位置合理，图片与脚注仍可用 | 通过 | 用户整体确认，未逐项反馈 |
| 旧书重解析 | 选设备已有旧导入记录，记下章节/段落再重新解析；恢复同处或提示近似恢复；原书仍可阅读 | 旧 0.1.0 导入样本通过 | 用户整体确认，未逐项反馈 |
| 取消 | 若重解析过程中可操作取消，取消后仍能打开旧书、继续阅读；完成过快则记录未测 | 未测：样本完成过快 | 用户整体确认，未逐项反馈 |

“返回原位置”检查与脚注中的书内回链不同：前者退出临时页回到原 Reader。本轮链接入口在菜单，正文文字没有内联点击能力。新样本重解析只能验证当前版本重解析，不能替代旧版本数据库升级。

## 收口边界

本轮不重扫用户小说、不发真实书源请求、不运行模拟器、不做发布。用户授权 ADB 后构建并覆盖安装正式入口 Release APK，保留现有应用数据。既有 iPhone 反馈发生在这些解析改动之前，不能直接复用。真实样本跨机缺失仍参照原报告单列。

PARSE-004 按用户手动验收结论收口；未测项目保持待验收或明确跳过，不能写成通过。后续功能不自动启动。

## Android ADB 真机记录

2026-09-09，BMH-AN10 / Android 12，1080×2400。真实手机，非模拟器。通过 UIAutomator 读取界面、ADB 点击/滑动和截图检查；没有向设备注入测试仓库或清理用户数据。

- 安装前为 0.1.0（versionCode 1）。先用系统文件选择器导入原创 EPUB，读到记录 02、约 9%；随后 `adb install -r` 覆盖成功。
- 本次 APK 为当前未提交工作树、HEAD `18bfcd30b6153d63fa76d13fd6b42f7a2d1cd11a`，正式入口 `lib/main.dart`、Release，应用版本仍为 1.0.0+2（不是已发布 1.1.0）。APK SHA-256：`21ed59104a6908020e3baadb94a4c8b8a5ea5cdf3c924954710cc5b155f50e50`。
- 升级后旧书架和 9% 位置保留；显式重解析提示位置已保留，仍从记录 02 开始。此次覆盖来自设备旧开发构建，不声称已发布 1.0.0 的所有升级路径通过。
- 本章链接打开脚注，返回原位置后记录 02～07 与 9% 不变；缺失目标显示不可用。
- 阅读至记录 20、51%，正常退出阅读器、force-stop 进程再启动，续读仍为记录 20、51%。之后目录锚点与图片仍可用。
- 第一章最后一页滑动进入第二章，未经过附录；目录可单独打开附录。没有做逐帧动画或性能测量，不将截图当作动画速度验收。
- UTF-8 / UTF-16 BOM 样本均通过系统文件选择导入，正文含 😀 / 𠮷，目录恰为两章，正文假标题未误切；UTF-8 第二章跳转正常。
- 下载内容提供器最初未列出 ADB 传输文件，切到手机存储 → Download → shiori-parse004 后可以选择；未更改系统文件提供器配置。
- 构建耗时约 292 秒，最终成功；日志包含本机 Android 36 annotations.zip 损坏警告及 Kotlin daemon 首次启动提示，未据此修改 SDK。该宿主环境问题不作为解析器缺陷，也不隐去。

验收截图已按用户要求移除，保留上述文字记录。取消与动画性能未测，iOS 用户已确认整体收口；不自动启动后续任务。

全新 EPUB 验证使用 `parse004-fresh.epub`：与仓库样本仅 OPF 书名改为“解析验收：新导入”，规避摘要去重；在当前构建通过文件选择器全新导入。样本留在手机 Download/shiori-parse004，原有书架未删除，供用户继续检查。
